import { withInstall } from '../../utils';

import ListItem from '../list/src/item';
import ListMeta from '../list/src/components/meta';
import ListAction from '../list/src/components/action';

export const CuListItem = withInstall(ListItem);
export const CuListMeta = withInstall(ListMeta);
export const CuListAction = withInstall(ListAction);
