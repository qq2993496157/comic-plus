import { withInstall } from '../../utils';

import { Container, Aside, Header, Main, Footer } from './src/container';

export const CuContainer = withInstall(Container);
export const CuAside = withInstall(Aside);
export const CuHeader = withInstall(Header);
export const CuMain = withInstall(Main);
export const CuFooter = withInstall(Footer);
