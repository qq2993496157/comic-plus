import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [vue()],
  build: {
    cssCodeSplit: true, // 启用 CSS 代码拆分功能
    target: 'modules',
    outDir: '../theme',
    assetsDir: '.', // 指定静态资源输出目录
    rollupOptions: {
      // input 中不包含 CSS 文件
      input: ['index.ts'],
      output: {
        // entryFileNames: '[name].js', // 指定输出文件名格式，保持不变
        assetFileNames: '[name][extname]' // 指定静态资源的文件名格式
      }
    }
  }
});
