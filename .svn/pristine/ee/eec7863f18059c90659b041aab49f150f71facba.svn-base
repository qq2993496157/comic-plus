.cu-date-picker__content {
  display: flex;
  width: 100%;
  height: 100%;
  align-items: center;
  padding: 0 8px;
}

.cu-date-picker__content .cu-input__inner {
  cursor: inherit;
}

.cu-date-picker__content .prefix-icon {
  min-width: 22px;
  padding-right: 8px;
  box-sizing: border-box;
  cursor: inherit;
  transition: transform 200ms;
  color: var(--cu-color-text-light);
}

.cu-date-picker__content .clearable {
  min-width: 22px;
  padding-left: 8px;
  box-sizing: border-box;
  color: var(--cu-color-text-light2);
}

.cu-date-picker__content .clearable i {
  cursor: pointer;
  display: none;
}
.cu-date-picker:hover .clearable i {
  display: initial;
}

.cu-date-picker__content .clearable i:hover {
  color: var(--cu-color-text);
}

.cu-date-picker__popper {
  font-size: 13px;
  color: var(--cu-color-text);
}
.cu-date-picker__popper.is-range .cu-date-picker__select {
  padding: 4px;
}

.cu-date-picker__select {
  width: 320px;
}
.cu-data-picker__select-head {
  padding: 8px;
  display: flex;
  align-items: center;
}

.cu-data-picker__select-head .head-content {
  flex: 1;
  justify-content: center;
  font-size: 16px;
  align-items: center;
  display: flex;
}

.cu-data-picker__select-head .head-content > span {
  padding: 0 4px;
  cursor: pointer;
}
.cu-data-picker__select-head .head-content > span.not-hover {
  cursor: initial;
}

.cu-data-picker__select-head i {
  font-size: 18px;
  cursor: pointer;
}

.cu-data-picker__select-head i:hover,
.cu-data-picker__select-head span:not(.not-hover):hover {
  color: var(--cu-color-primary);
}

.cu-data-picker__select-content {
  width: 100%;
}
.cu-data-picker__select-content > div {
  display: flex;
}
.cu-data-picker__select-week {
  font-size: 14px;
  width: 100%;
  display: flex;
  border-bottom: 1px solid var(--cu-border-color);
}

.cu-data-picker__select-week > span {
  flex: 1;
  height: 32px;
  padding: 4px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  box-sizing: border-box;
}
.cu-data-picker__select-days {
  flex-direction: column;
}
.cu-data-picker__select-days > div {
  width: 100%;
  display: flex;
}
.cu-data-picker__select-days > div + div {
  margin-top: 8px;
}

.cu-data-picker__select-days > div > span {
  position: relative;
  flex: 1;
  height: 32px;
  padding: 4px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  box-sizing: border-box;
}

.cu-data-picker__select-days > div > .is-notcur:not(.is-disabled) {
  color: var(--cu-color-text-light);
}
.cu-data-picker__select-month,
.cu-data-picker__select-year {
  flex-wrap: wrap;
}
.cu-data-picker__select-month > div,
.cu-data-picker__select-year > div {
  width: 25%;
  height: 60px;
  line-height: 60px;
  text-align: center;
  cursor: pointer;
}

.cu-data-picker__select-days span:hover,
.cu-data-picker__select-month > div:hover,
.cu-data-picker__select-year > div:hover {
  background-color: var(--cu-background-color);
}

.cu-data-picker__select-content .is-selected {
  color: var(--cu-color-primary);
}

.cu-data-picker__select-content .is-disabled {
  background-color: var(--cu-disabled-background);
  color: var(--cu-disabled-font);
  cursor: no-drop;
}

.cu-data-picker__select-days .is-selected:not(.is-notcur)::after {
  content: '';
  display: block;
  position: absolute;
  left: 50%;
  bottom: 4px;
  transform: translateX(-50%);
  width: 30%;
  height: 2px;
  background-color: var(--cu-color-primary);
}

.cu-date-picker__footer {
  border-top: 1px solid var(--cu-border-color);
  padding: 4px 8px;
  display: flex;
  justify-content: flex-end;
}

.cu-date-table__box {
  padding: 12px;
  display: flex;
}
.cu-date-table__box > div + div {
  border-left: 1px solid var(--cu-border-color);
}
.cu-date-table__box .cu-date-table__box {
  padding: 0;
}

.cu-date-table__box .shortcuts {
  padding: 0 4px;
  width: 80px;
  font-size: 12px;
  line-height: 22px;
  display: flex;
  flex-direction: column;
}
.cu-date-table__box .shortcuts > span:hover {
  color: var(--cu-color-primary);
  cursor: pointer;
}

.cu-date-picker__popper.small {
  font-size: 12px;
}
.cu-date-picker__popper.small .cu-data-picker__select-days > div > span {
  height: 28px;
}
.cu-date-picker__popper.small .cu-date-picker__select {
  width: 280px;
}

.cu-date-picker__popper.small .cu-data-picker__select-week {
  font-size: 13px;
}

.cu-date-picker__popper.small .cu-data-picker__select-head {
  padding: 4px;
}

.cu-date-picker__popper.small .cu-date-table__box {
  padding: 8px;
}

.cu-date-picker__popper.small .cu-data-picker__select-head .head-content {
  font-size: 14px;
}

.cu-date-picker__popper.small .cu-data-picker__select-days > div + div {
  margin-top: 2px;
}
