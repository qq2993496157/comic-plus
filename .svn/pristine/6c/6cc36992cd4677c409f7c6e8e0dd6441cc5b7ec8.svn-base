.cu-badge {
  position: relative;
  --color: #fff;
  display: inline-flex;
}
.cu-badge__inner {
  border: 1px solid #fff;
  display: inline-block;
  padding: 2px 6px;
  color: #fff;
  background-color: var(--color);
  font-size: 12px;
  line-height: 1;
  border-radius: 10px;
  z-index: 1;
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
  cursor: default;
  position: relative;
}
.cu-badge__inner.position {
  position: absolute;
  right: 10px;
  top: 0;
  transform: translate(100%, -50%);
}
.cu-badge__inner.position.dot {
  padding: 0;
  width: 10px;
  height: 10px;
  right: 5px;
}
.cu-badge.primary {
  --color: var(--cu-color-primary);
}
.cu-badge.success {
  --color: var(--cu-color-success);
}
.cu-badge.warning {
  --color: var(--cu-color-warning);
}
.cu-badge.danger {
  --color: var(--cu-color-danger);
}
.cu-badge.info {
  --color: var(--cu-color-info);
}

.comic-plus__is-comic .cu-badge .cu-badge__inner {
  border-color: var(--cu-comic-border-color);
}
