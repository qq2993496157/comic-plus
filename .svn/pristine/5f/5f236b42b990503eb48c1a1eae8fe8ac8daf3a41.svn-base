import { defineComponent, h } from 'vue';
import '../style/button.css';

export default defineComponent({
  name: 'CuButtonGroup',
  props: {
    round: Boolean
  },
  setup(props, { slots }) {
    return () => h('span', { class: ['cu-button-group', { round: props.round }] }, slots);
  }
});
