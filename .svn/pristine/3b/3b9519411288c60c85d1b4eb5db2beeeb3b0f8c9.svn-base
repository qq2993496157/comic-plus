import type { ExtractPropTypes } from 'vue';

export const dropdownProps = {
  trigger: {
    type: String,
    default: 'hover',
    validator: (value: string) => ['hover', 'click', 'right-click'].includes(value)
  },
  chooseAfterHide: {
    type: Boolean,
    default: true
  },
  button: Boolean,
  size: String,
  buttonType: {
    type: String,
    default: 'primary'
  },
  buttonIcon: {
    type: String,
    default: 'cu-icon-down'
  }
} as const;

export type DropdownProps = ExtractPropTypes<typeof dropdownProps>;

export const dropdownEmits = {
  ['menu-click']: (val?: string) => true,
  ['button-click']: (evt: MouseEvent) => evt instanceof MouseEvent
};

export type DropdownEmits = typeof dropdownEmits;
