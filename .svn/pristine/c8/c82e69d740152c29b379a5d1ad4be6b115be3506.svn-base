import { defineComponent, h } from "vue";
import '../style/css';

export default defineComponent({
  name: 'CuButtonGroup',
  setup(props, { slots }) {
    return () => h('span', { class: 'cu-button-group' }, slots)
  }
})