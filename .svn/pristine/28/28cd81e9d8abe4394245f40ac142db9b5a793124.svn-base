import { useAlert, AlertOptions } from './alert/main';
import { useConfirm, ConfirmOptions } from './confirm/main';
import { useMessage, MessageOptions } from './message/main';
import { useNotice, NoticeOptions } from './notice/main';

type Message = {
  alert: (options: AlertOptions) => Promise<any>;
  confirm: (options: ConfirmOptions) => Promise<any>;
  message: (options: MessageOptions) => void;
  notice: (options: NoticeOptions) => void;
};

const CuMessageBox: Message = {
  alert: useAlert,
  confirm: useConfirm,
  message: useMessage,
  notice: useNotice
};

export default CuMessageBox;
