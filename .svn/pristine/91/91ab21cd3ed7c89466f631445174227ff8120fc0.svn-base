.cu-radio {
  margin-right: 10px;
  display: inline-block;
  font-size: 14px;
  color: var(--font);
  border-color: var(--border);
  cursor: pointer;
  vertical-align: middle;
}
.cu-radio__inner {
  margin-right: 4px;
  display: inline-flex;
  line-height: 1;
  width: 16px;
  height: 16px;
  border: 2px solid;
  border-color: inherit;
  border-radius: 50%;
  position: relative;
}
.cu-radio__inner::after {
  content: '';
  display: block;
  width: 9px;
  height: 9px;
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  background-color: var(--border);
  border-radius: 50%;
  display: none;
}
.cu-radio:hover {
  color: var(--active-color, var(--primary));
  border-color: var(--active-color, var(--primary));
}

.cu-radio.is-disabled {
  cursor: no-drop;
}
.cu-radio .cu-radio__label {
  color: inherit;
}

.cu-radio input:not(:disabled):active ~ .cu-radio__inner {
  transform: scale(0.9);
}
.cu-radio input:checked ~ .cu-radio__inner {
  border-color: var(--active-color, var(--primary));
}

.cu-radio input:checked ~ .cu-radio__inner::after {
  background-color: var(--active-color, var(--primary));
  display: block;
}
.cu-radio input:checked ~ .cu-radio__label {
  color: var(--active-color, var(--primary));
}

.cu-radio input:disabled ~ .cu-radio__inner,
.cu-radio input:disabled ~ .cu-radio__label {
  color: var(--disabled-font);
}
.cu-radio input:disabled ~ .cu-radio__inner {
  background-color: var(--disabled-bg);
  border-color: var(--disabled-font);
}

.cu-radio-group {
  display: inline-flex;
  flex-wrap: wrap;
}
