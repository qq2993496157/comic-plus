<template>
  <span class="cu-upload__chooseItem" @click="emit('choose')">
    <slot name="trigger">
      <cu-button size="small" type="primary" v-if="type === 'list'">选择文件</cu-button>
      <div class="cu-upload__trigger" v-else>
        <i class="cu-icon-add"></i>
      </div>
    </slot>
  </span>
  <span class="cu-upload__chooseItem">
    <slot></slot>
  </span>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import CuButton from '../../button';

defineOptions({
  name: 'CuUploadChoose'
});

const props = defineProps({
  type: {
    type: String,
    required: true
  }
});

const emit = defineEmits(['choose']);
</script>

<style scoped></style>
