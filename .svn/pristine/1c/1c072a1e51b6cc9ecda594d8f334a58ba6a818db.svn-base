.cu-anchor {
  list-style: none;
  font-size: 12px;
  padding-left: 6px;
  color: var(--cu-color-text-light);
  border-left: var(--cu-anchor-line-weight, 4px) solid transparent;
  border-bottom: 0;
  position: relative;
  --cu-anchor-line-height: 14px;
  --cu-anchor-height: calc(var(--cu-size-default) - 4px);
  --cu-anchor-line-offset: 7px;
}

.cu-anchor.cu-anchor--horizontal {
  padding-left: 0;
  display: flex;
  flex-wrap: nowrap;
  border-left: 0;
  border-bottom: var(--cu-anchor-line-weight, 2px) solid transparent;
}

.cu-anchor.show-line {
  border-color: var(--cu-border-color);
}

.cu-anchor .cu-anchor__line {
  display: block;
  position: absolute;
  border-radius: calc(var(--cu-anchor-line-weight, 4px) / 2);
  background-color: var(--cu-anchor-color, var(--cu-color-primary));
  transition: all 200ms;
}

.cu-anchor.cu-anchor--vertical .cu-anchor__line {
  width: var(--cu-anchor-line-weight, 4px);
  left: calc(0px - var(--cu-anchor-line-weight, 4px));
  height: var(--cu-anchor-line-height);
  transform: translateY(var(--cu-anchor-line-offset));
}

.cu-anchor.cu-anchor--horizontal .cu-anchor__line {
  bottom: 0;
  height: var(--cu-anchor-line-weight, 2px);
}

.cu-anchor-link {
  display: inline-block;
  list-style: none;
  cursor: pointer;
  color: inherit;
  transition: all 200ms;
  font-size: inherit;
}

.cu-anchor-link.underline {
  text-decoration: underline;
}

.cu-anchor .cu-anchor-link {
  display: block;
  min-height: var(--cu-anchor-height);
  line-height: var(--cu-anchor-height);
  text-decoration: none;
}

.cu-anchor-sub-link {
  padding-left: 8px;
}

.cu-anchor-link a.active {
  color: var(--cu-anchor-color, var(--cu-color-primary));
}
.cu-anchor-link a:hover {
  text-shadow: 0 0 1px;
}

.cu-anchor-link a {
  text-decoration: none;
  color: inherit;
  width: 100%;
  height: 100%;
  display: block;
}
.cu-anchor .cu-anchor-link a {
  padding: 0 4px;
}

.cu-anchor.small {
  --cu-anchor-line-height: 10px;
  --cu-anchor-height: calc(var(--cu-size-small) - 4px);
  --cu-anchor-line-offset: 5px;
}
.cu-anchor.large {
  --cu-anchor-line-height: 18px;
  --cu-anchor-height: calc(var(--cu-size-large) - 8px);
  --cu-anchor-line-offset: 7px;
}

.cu-anchor.large .cu-anchor-link {
  font-size: 14px;
}
